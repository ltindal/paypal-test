# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'venmo-draft' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for venmo-draft

  target 'venmo-draftTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'PayPal-iOS-SDK'

  end

  target 'venmo-draftUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
